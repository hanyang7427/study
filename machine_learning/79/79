二十三、寻找最优超参数
将多个超参数组合形成列表，用学习算法自动找到其中的最优组合，并用该组合所对应的模型用于数据分析。
sklearn.model_selection.GridSearchCV(模型, 参数列表字典, cv=交验)
--> 最好模型
2个超参数：A B
          A
       1 2 3
    4  a b c
B   5  d e f
    6  g h i
代码：svm_bhp.py
二十四、事件预测器
利用支持向量机的回归器SVR对非线性样本进行回归预测。
代码：event.py
二十五、基于k-means算法的聚类
利用中心点优化，总是视图让族群中的各个数据点与中心点的距离只和最小化。
代码：kmeans.py
二十六、基于聚类的矢量量化压缩
用聚类的中心替代聚类中所有样本，达到减小信息量的压缩效果。
代码：quant.py
二十七、基于均值漂移算法的聚类
假设有一组输入点，均值漂移算法会把这些点视作服从某种概率密度函数的样本，如果这些数据点存在集群，那么对应概率密度函数的峰值就是它们的中心。
代码：shift.py
二十八、基于凝聚层次算法的聚类
最开始，将每个数据样本视作独立的族群，然后不断根据临近原则，将多个点凝聚为一个族群，经过多轮凝聚迭代，不断减少族群数量，直到满足要求为止。
代码：agglo.py、conn.py
